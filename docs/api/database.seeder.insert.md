<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@monorepo-framework/database](./database.md) &gt; [Seeder](./database.seeder.md) &gt; [insert](./database.seeder.insert.md)

## Seeder.insert() method

Insert rows, chunked so large seed sets stay within parameter limits.

**Signature:**

```typescript
protected insert(table: string, rows: readonly Row[]): Promise<void>;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

table


</td><td>

string


</td><td>

Table name, unquoted.


</td></tr>
<tr><td>

rows


</td><td>

readonly [Row](./database.row.md)<!-- -->\[\]


</td><td>

Rows to insert; an empty array is a no-op.


</td></tr>
</tbody></table>

**Returns:**

Promise&lt;void&gt;

## Remarks

Column names come from the first row, and every row is assumed to share that shape — a later row with extra keys silently loses them, and one missing a key binds `undefined`<!-- -->, i.e. `NULL`<!-- -->.

Rows are sent in chunks of 500 to stay under Postgres' 65535-parameter cap. The chunks are separate statements, so a failure part-way through leaves earlier chunks inserted unless the caller wrapped the seeder in a transaction.

When the rows carry an explicit `id`<!-- -->, the table's Postgres sequence is resynced afterwards so the next application insert does not collide.

