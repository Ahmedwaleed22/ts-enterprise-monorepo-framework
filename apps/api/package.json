{
  "name": "@monorepo-framework/api",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "dev": "tsx --watch --watch-preserve-output src/index.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "db": "pnpm --filter @monorepo-framework/database run build && tsx src/database/console.ts",
    "migrate": "pnpm run db migrate",
    "db:seed": "pnpm run db db:seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@monorepo-framework/database": "workspace:*",
    "express": "^5.2.1",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/pg": "^8.20.4",
    "tsx": "^4.23.11"
  }
}
