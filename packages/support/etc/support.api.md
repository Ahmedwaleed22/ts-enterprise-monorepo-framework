## API Report File for "@monorepo-framework/support"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export type Clock = () => number;

// @public
export interface Closable {
    close(): Promise<void> | void;
}

// @public
export type DriverFactory<TDriver, TContext> = (context: TContext) => TDriver | Promise<TDriver>;

// @public
export function envBoolean(env: NodeJS.ProcessEnv, key: string, fallback?: boolean): boolean;

// @public
export function envChoice<T extends string>(env: NodeJS.ProcessEnv, key: string, aliases: Record<string, T | undefined>, fallback: T): T;

// @public
export function envNumber(env: NodeJS.ProcessEnv, key: string, fallback: number): number;

// @public
export function envString(env: NodeJS.ProcessEnv, key: string, fallback: string): string;

// @public
export function loadDriver<T>(specifier: string, installHint: string): Promise<T>;

// @public
export abstract class Manager<TDriver, TContext> {
    protected constructor(
    context: TContext, fallback: string);
    close(): Promise<void>;
    protected readonly context: TContext;
    driver(name?: string): Promise<TDriver>;
    extend(name: string, factory: DriverFactory<TDriver, TContext>): this;
    // @virtual
    protected abstract factories(): Record<string, DriverFactory<TDriver, TContext> | undefined>;
}

// @public
export type Output = (message: string) => void;

// @public
export const silent: Output;

// @public
export const systemClock: Clock;

```
