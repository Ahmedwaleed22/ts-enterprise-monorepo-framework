## Public API Report File for "@monorepo-framework/database"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export type Bindable = string | number | boolean | Date | null | undefined;

// @public
export class Blueprint {
    constructor(
    table: string,
    mode?: BlueprintMode);
    bigInteger(name: string): ColumnDefinition;
    boolean(name: string): ColumnDefinition;
    readonly columns: ColumnDefinition[];
    date(name: string): ColumnDefinition;
    decimal(name: string, precision?: number, scale?: number): ColumnDefinition;
    float(name: string): ColumnDefinition;
    foreignId(name: string): ColumnDefinition;
    id(name?: string): ColumnDefinition;
    index(columns: string | string[], name?: string): this;
    integer(name: string): ColumnDefinition;
    json(name: string): ColumnDefinition;
    readonly mode: BlueprintMode;
    softDeletes(name?: string): ColumnDefinition;
    string(name: string, length?: number): ColumnDefinition;
    readonly table: string;
    text(name: string): ColumnDefinition;
    timestamp(name: string): ColumnDefinition;
    timestamps(): void;
    unique(columns: string | string[], name?: string): this;
    uuid(name: string): ColumnDefinition;
}

// @public
export type BlueprintMode = 'create' | 'alter';

// @public
export class ColumnDefinition {
    constrained(table: string, column?: string): this;
    default(value: Bindable | RawExpression): this;
    index(value?: boolean): this;
    nullable(value?: boolean): this;
    onDelete(action: ReferentialAction): this;
    onUpdate(action: ReferentialAction): this;
    primary(value?: boolean): this;
    references(table: string, column?: string): this;
    unique(value?: boolean): this;
    unsigned(value?: boolean): this;
}

// @public
export type ColumnType = 'increments' | 'string' | 'text' | 'integer' | 'bigInteger' | 'boolean' | 'float' | 'decimal' | 'date' | 'timestamp' | 'json' | 'uuid';

// @public
export function configFromEnv(env?: NodeJS.ProcessEnv): ConnectionConfig;

// @public
export interface Connection {
    close(): Promise<void>;
    readonly dialect: Dialect;
    select<T extends Row = Row>(query: string, bindings?: readonly Bindable[]): Promise<T[]>;
    statement(query: string, bindings?: readonly Bindable[]): Promise<void>;
    transaction<T>(callback: (connection: Connection) => Promise<T>): Promise<T>;
}

// @public
export interface ConnectionConfig {
    database?: string;
    dialect: Dialect;
    host?: string;
    password?: string;
    port?: number;
    ssl?: boolean;
    url?: string;
    username?: string;
}

// @public
export interface ConsoleOptions {
    connection?: ConnectionConfig;
    migrations: string;
    migrationsTable?: string;
    packageMigrations?: readonly MigrationSource[];
    seeders: string;
}

// @public
export function createConnection(config?: ConnectionConfig): Promise<Connection>;

// @public
export type Dialect = 'postgres' | 'mysql' | 'sqlite';

// @public
export abstract class Migration {
    // @virtual
    abstract down(schema: Schema, connection: Connection): Promise<void> | void;
    // @virtual
    abstract up(schema: Schema, connection: Connection): Promise<void> | void;
}

// @public
export interface MigrationRecord {
    batch: number;
    migration: string;
}

// @public
export class MigrationRepository {
    constructor(connection: Connection, table?: string);
    ensureExists(): Promise<void>;
    lastBatch(): Promise<number>;
    log(migration: string, batch: number): Promise<void>;
    migrationsInBatch(batch: number): Promise<string[]>;
    nextBatch(): Promise<number>;
    ran(): Promise<string[]>;
    ranRecords(): Promise<MigrationRecord[]>;
    remove(migration: string): Promise<void>;
}

// @public
export interface MigrationSource {
    path: string;
    prefix?: string;
}

// @public
export interface MigrationStatus {
    batch: number | undefined;
    migration: string;
    ran: boolean;
}

// @public
export class Migrator {
    constructor(connection: Connection, options: MigratorOptions);
    available(): Promise<string[]>;
    fresh(): Promise<string[]>;
    reset(): Promise<string[]>;
    rollback(steps?: number): Promise<string[]>;
    run(): Promise<string[]>;
    status(): Promise<MigrationStatus[]>;
}

// @public
export interface MigratorOptions {
    logger?: (message: string) => void;
    path: string | readonly MigrationSource[];
    table?: string;
}

// @public
export function normalizeDialect(value: string): Dialect;

// @public
export function raw(value: string): RawExpression;

// @public
export interface RawExpression {
    readonly value: string;
}

// @public
export type ReferentialAction = 'cascade' | 'restrict' | 'set null' | 'no action';

// @public
export type Row = Record<string, unknown>;

// @public
export function runConsole(argv: string[], options: ConsoleOptions): Promise<number>;

// @public
export class Schema {
    constructor(connection: Connection);
    create(table: string, define: (table: Blueprint) => void): Promise<void>;
    get dialect(): Dialect;
    drop(table: string): Promise<void>;
    dropAllTables(): Promise<void>;
    dropIfExists(table: string): Promise<void>;
    hasTable(table: string): Promise<boolean>;
    raw(query: string): Promise<void>;
    table(table: string, define: (table: Blueprint) => void): Promise<void>;
    tableNames(): Promise<string[]>;
}

// @public
export abstract class Seeder {
    constructor(
    connection: Connection);
    protected call(...seeders: SeederConstructor[]): Promise<void>;
    protected readonly connection: Connection;
    protected insert(table: string, rows: readonly Row[]): Promise<void>;
    // @virtual
    abstract run(): Promise<void>;
    protected truncate(table: string): Promise<void>;
}

// @public
export type SeederConstructor = new (connection: Connection) => Seeder;

// @public
export class SeederRunner {
    constructor(connection: Connection, options: SeederRunnerOptions);
    run(name?: string): Promise<void>;
}

// @public
export interface SeederRunnerOptions {
    defaultSeeder?: string;
    logger?: (message: string) => void;
    path: string;
}

```
